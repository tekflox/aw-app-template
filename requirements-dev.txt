pytest-asyncio>=0.23  # only if your tests are async (real sockets, asyncio servers)
pytest-cov>=5.0  # .github/workflows/test.yml's coverage gate (pyproject.toml [tool.coverage.report] fail_under=80)
